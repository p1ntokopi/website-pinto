- Prefers to plan before implementing — uses /plan mode and writes detailed plans with spec tables, architecture diagrams, and verification steps; explicitly requests the plan/report first ("laporan plan dulu") before any implementation work begins. Confidence: 0.85
- Plan documents explicitly flag decisions as "User Review Required" / "Open Questions" — expects the assistant to confirm those with the user before writing any code. Confidence: 0.8
- Wants the assistant to proactively audit the system for missing/half-built features ("apa yang kurang lengkap") — expects a gap analysis of existing DB/backend capabilities vs. missing UI/flows, backed by file-path evidence, delivered as a prioritized phased roadmap for review before greenlighting new feature work. External blockers (e.g., Xendit still in verification) are a reason to defer that work and prioritize unblocked alternatives instead. Confidence: 0.7
- Primary cashier/POS device is an Android phone — web features must target Android Chrome (e.g., Web Serial support only landed in Chrome 138+ there). Confidence: 0.8
- Prefers manual, explicit user actions over automation, and keeps existing behavior as a fallback when adding new integrations (chose manual "Cetak Struk" button over auto-print on order; kept browser `window.print()` as fallback). Confidence: 0.6
- When the user supplies reference/pasted component code (e.g., a showcase UI component), integrate it into the project — preserve its core API and behavior (compound components, animation timings) while converting it to TypeScript and adapting its styling to the project's design system/tokens (Pinto paper/ink/coffee palette instead of generic grays). Confidence: 0.85
- Wants a single shared component to serve both buyer-facing and admin surfaces when the same feature appears in both (e.g., receipt printer visual on `/t/[slug]/order/[orderNumber]` and the admin print view), differentiated via props rather than duplicated per surface. Confidence: 0.7
- Works in milestone-sized specs (e.g., 'MASTER PROMPT M5') that carry explicit non-goals ('Do NOT rebuild the existing application', 'Do NOT implement Xendit yet', 'Do NOT duplicate tables') — inspect the existing schema/architecture first, reuse it, and strictly respect the stated non-goals instead of over-implementing. Confidence: 0.85
- Sensitive/financial data must be protected server-side, never just by hiding nav items: RLS deny-by-default for non-owner roles, server-side route guards, authorization checks inside SECURITY DEFINER RPCs / server actions, audit trail written by DB triggers, and soft delete (ACTIVE/VOIDED) instead of hard deletion. Confidence: 0.85
- Financial reporting rules: all pages reconcile through one SQL aggregation source (a single `get_financial_summary` RPC computed in the DB, not rows pulled to the browser); cash basis on `payments.paid_at`; metrics labeled honestly ('Estimasi Laba', never 'Laba Bersih', until COGS exists) with explicit Indonesian definition footnotes on every page. Confidence: 0.8
- Avoids hardcoding personal data (e.g., account emails) in migration files — prefers placeholder SQL plus a runbook the user executes manually instead. Confidence: 0.7
- P1NTO role model (user-corrected in plan review): only two real roles exist — **admin** (merangkap kasir, dapur/kitchen, dan semua operasional) and **owner**. There are no separate staff/kitchen users (legacy enum values stay unused in the DB); operational/cashier features are built for admin & owner only, and owner is treated as a superset of admin for permissions. Confidence: 0.9
- Greenlights each roadmap phase with terse casual commands ("gas fase 2", "gas fase 3") — treat this as approval to execute the entire phase autonomously (todo list → implement → verify → summary) without pausing for further confirmation, then report back and wait for the next go-ahead. The message may be mostly auto-attached IDE context; the short command text is the actual instruction. Confidence: 0.75
- After a phase ships, the user manually QA-tests in the browser/device and asks the assistant for the list of where to check what ("cek tampilan dan tes perlu dimana saja") — provide a structured manual QA checklist per surface (prerequisites → route → expected result), covering public pages, customer flow, cashier, kitchen, owner area, RLS/access-control (e.g., admin console query returning empty array), responsive widths, and an end-to-end reconcile with identical numbers across Ringkasan/Ikhtisar/Laporan; flag the highest-priority checks and ask for screenshots/error messages on failure. Confidence: 0.65
- Stale-client-state handling on multi-tab/multi-device ops surfaces (kitchen display vs. order detail): treat server-side "invalid transition" rejections as stale-UI symptoms, not logic bugs — on mutation failure, revert optimistic updates to the true status and auto-refresh (`router.refresh()`) so visible action buttons match current server state, paired with the humanized error toast. Confidence: 0.65
- When the user pastes a runtime error with no explanation (repeated pattern: invalid-transition toast, generic finance load failure, 404 on order detail), respond with the code fix plus an ordered diagnosis checklist: most-likely cause first with a probability estimate, exact commands/SQL to run (e.g., Supabase SQL Editor `NOTIFY pgrst, 'reload schema'`, direct RPC test `select public.get_financial_summary(...)`, existence check by id), what result distinguishes each cause (branching outcomes), and what to send back (console error text) if still failing. Confidence: 0.7
- Database fixes follow an append-only numbered-migration convention: bugs in already-applied SQL are fixed by a NEW migration file redefining the object (e.g., `0024_finance_rpc_fix.sql` uses `create or replace function` to fix the 0022 RPC, matching the 0018/0019 convention) — existing migration files are never edited retroactively. Confidence: 0.7
- Migrations are applied manually by the user via Supabase SQL Editor (no migration CLI/CI) — deliver each migration as a ready-to-run .sql file, state whether a PostgREST schema reload (`NOTIFY pgrst, 'reload schema'`) is needed, and include a post-run verification query the user can paste to confirm success (e.g., `select public.get_financial_summary(current_date, current_date);`). Confidence: 0.7
