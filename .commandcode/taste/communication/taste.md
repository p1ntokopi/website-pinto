- Prefers to communicate in Bahasa Indonesia. Confidence: 0.85
- Project-facing text — UI copy, empty/error states, validation messages, and code comments — is written in Bahasa Indonesia (e.g., 'Belum ada pengeluaran pada periode ini.', 'Gagal menyimpan pengeluaran. Coba lagi.'). Error messages are humanized for end users: state the plain-language cause and the next step ('Status pesanan sudah berubah menjadi "Siap". Halaman akan dimuat ulang — cek status terbaru lalu coba lagi.'), never raw technical internals like enum transitions or role names ('Invalid transition from READY to CONFIRMED for role admin'). Confidence: 0.85
- Error surfacing must never mask the root cause behind a generic fallback (e.g., bare 'Gagal memuat data keuangan. Coba lagi.') or a blanket 404: in server components, `notFound()` is reserved for genuine zero-rows (PostgREST `PGRST116`); other query failures (RLS, embed) render an in-page Indonesian error state with the cause logged to console under a searchable tag (e.g., `[order detail]`). Surface the original server error to the UI truncated (~140 chars), log it to console with a searchable tag (e.g., `[get_financial_summary]`) so the user can paste it back, and translate known infrastructure errors (PostgREST `schema cache` / PGRST202, `permission denied`) into concrete Indonesian remediation steps (e.g., run `NOTIFY pgrst, 'reload schema'` in Supabase SQL Editor). Validated in practice: the surfaced original error + console tag is exactly what the user pasted back, pinpointing the true Postgres bug (a GROUP BY violation in the RPC); the same pattern was since extended to order-detail 404s. Confidence: 0.8
